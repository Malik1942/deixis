# Locant v0.1: the pointing flow

**Scope of this spec:** the 3-hour version, tagged `v0.1`. One hotkey, one click, one payload.
**Revision:** Sep 13, 2026, after the AX spike. Demo app is Oryne.
**Out of scope (v0.2+):** MCP server, drag selection, floating ball, OCR, color picker, cutout, 30-day lifecycle, Settings UI, Sparkle, Safari/Chrome tab URL (deferred to v0.2; `url` stays null).

Read `CLAUDE.md` first. This file is the task contract.

---

## 0. What changed since the PRD

Competitive research (Sep 12, 2026) moved three things:

1. **Window-level context is already a platform feature.** Codex Appshots (Command-Command) sends the front window's screenshot plus accessibility text to a Codex thread. Claude Desktop quick entry (Option-Option) attaches a region or window to a Claude chat. EYHN/appshots clones this for Claude Code. So v0.1 does **not** do window capture. It does element capture only. Pitch line: "Appshots gives your agent the window. Locant gives it the element."
2. **MCP image content blocks are unreliable across agents.** Claude Code and Cursor consume them. Codex has two open issues (model cannot see the image; `content[]` dropped when `structuredContent` is present). Gemini CLI unconfirmed. Therefore the payload's primary form is **text plus an absolute image path**, and v0.1 writes a JSON sidecar so the v0.2 MCP server needs no app changes.
3. **The element schema must be platform-neutral.** Windows UI Automation and macOS Accessibility expose the same concepts (role, name, identifier, frame, ancestry). Field names use no `AX` prefix so a future Windows port shares the schema and the MCP server unchanged.

4. **The AX spike passed (Sep 13).** Per-app hit testing reaches the Simulator's app tree through public API; frames are in screen points; the tree is built lazily; SF Symbol names show up as identifiers. R3, the schema, and the tests reflect all four findings.

Double-tap modifier keys are now the convention for "send context to agent" (Codex: ⌘⌘, Claude: ⌥⌥). Locant defaults to **double-tap Control** to avoid both.

## 1. Goal

Press the hotkey, click one element in any app, optionally type a note, press Enter. The clipboard now holds a Markdown payload (and the PNG) that lets a coding agent find the right file. Demo target: the primary capture button in the Oryne iOS Simulator with `.accessibilityIdentifier("captureButton")` (added Sep 13), pasted into Claude Code, agent edits the correct view on the first try.

## 2. Time plan (3 hours, timer on, commits carry timestamps)

| Slot | Work | Exit condition |
|---|---|---|
| 0:00 to 0:10 | **Confirm the system-wide fallback.** Spike passed Sep 13 via the per-app path (Oryne in Simulator, `captureButton` readable, frame correct in screen points). Run a throwaway probe script (scratch, not committed) with Simulator frontmost to confirm the system-wide hit test also works. | Result noted in `README.md` under "Known limitations". Hard stop at 0:10; the fallback is not load-bearing. |
| 0:10 to 0:45 | `Resolve/ElementResolver.swift` (per-app path, lazy-tree retry, identifierSource) + tests with recorded AX fixtures | 8 tests green |
| 0:45 to 1:30 | `Capture/`: hotkey, live selection overlay with hover readout, window hit-test for the target PID, coordinate functions + tests, ScreenCaptureKit crop at click | Overlay appears, hover shows readout, click resolves, Esc cancels, app and overlay excluded from image |
| 1:30 to 2:15 | `Payload/`: `MarkdownBuilder` + tests, `PasteboardWriter`, `Store/FileStore` (PNG + JSON sidecar) | Paste into Claude Code shows Markdown; paste into Preview shows image |
| 2:15 to 2:45 | Note field (Enter commits, Esc cancels), failure feedback, menu bar item with Quit | End-to-end run on demo target |
| 2:45 to 3:00 | `git tag v0.1`, README with permissions steps | Tag pushed |

If a slot overruns by more than 10 minutes, cut the next slot's polish, not its core.

## 3. Repository layout

```
locant/
  Locant/                      Xcode project, macOS 15+, Swift 6, strict concurrency = complete
    App/
      LocantApp.swift          @main, LSUIElement, menu bar item, permission alerts
      AppState.swift           @Observable AppState, the only shared object; runs the capture session
    Capture/
      HotkeyMonitor.swift      double-tap Control via NSEvent global + local monitors
      SelectionOverlay.swift   transparent non-activating NSPanel per display, live hover highlight + label, note field, toast, Esc; design tokens from PRD §6
      ScreenCapture.swift      SCScreenshotManager crop at click time, excludes own windows
      Geometry.swift           pure coordinate functions: AppKit ↔ CG points, crop padding + clamp, pixel rects, window hit-test
      ContextCollector.swift   front app bundle id + name, window title, Simulator device + app bundle id (URL deferred to v0.2)
    Resolve/
      AccessibilityReader.swift actor owning every AXUIElement: hit test, attribute read, ancestor walk → ElementSnapshot
      ElementResolver.swift    ElementSnapshot → ResolvedElement (pure transform, testable) + lazy-tree retry
      ModeClassifier.swift     fix vs reference from CaptureContext
    Payload/
      MarkdownBuilder.swift    Capture → String (pure)
      PasteboardWriter.swift   PNG + Markdown on one NSPasteboardItem
    Store/
      FileStore.swift          writes locant-<slug>-<stamp>.png and .json
    Model/
      Capture.swift            Codable value types mirroring schema/capture.schema.json
      CaptureError.swift
  LocantTests/
    ElementResolverTests.swift
    MarkdownBuilderTests.swift
    GeometryTests.swift
    FileStoreTests.swift
    Fixtures/                  recorded AX snapshots as JSON
  schema/
    capture.schema.json        the contract shared with the future MCP server
  README.md
```

No third-party dependencies. No Settings window in v0.1 (hotkey and folder are constants).

## 4. Data model (`schema/capture.schema.json`, v1)

```json
{
  "schemaVersion": 1,
  "id": "20260912-140312-k7q2",
  "createdAt": "2026-09-12T14:03:12-07:00",
  "mode": "fix",
  "image": {
    "path": "/Users/malik/Pictures/Locant/locant-simulator-20260912-140312-k7q2.png",
    "widthPt": 144, "heightPt": 112, "scale": 2,
    "crop": { "x": 272, "y": 48, "w": 144, "h": 112 }
  },
  "source": {
    "app": { "bundleId": "com.apple.iphonesimulator", "name": "Simulator" },
    "window": { "title": "iPhone 17 Pro" },
    "url": null,
    "simulator": { "device": "iPhone 17 Pro", "appBundleId": "com.malikzhang.oryne" }
  },
  "element": {
    "role": "button",
    "rawRole": "AXButton",
    "label": "Capture",
    "identifier": "captureButton",
    "identifierSource": "declared",
    "value": null,
    "frame": { "x": 312, "y": 88, "w": 64, "h": 32 },
    "path": [
      { "role": "group", "identifier": null },
      { "role": "toolbar", "identifier": null },
      { "role": "button", "identifier": "captureButton" }
    ]
  },
  "note": "make this rounded, match the other toolbar buttons",
  "ocr": null,
  "iterations": [],
  "resolved": false
}
```

Rules:
- `element` is `null` when resolution fails. Never fabricate. The one computed element, `cluster`, is labeled as such in `role` and `rawRole` and carries `members`.
- `MyApps` in v0.1: `com.inspireocean.app` (Oryne), `com.malik.Moti`.
- `role` values are lowercase, platform-neutral (`button`, `textField`, `staticText`, `image`, `group`, `navigationBar`, `unknown`). `ElementResolver` maps `AXButton` → `button` etc. Unknown AX roles map to `unknown`. `rawRole` always carries the platform's own role string.
- `id` is `yyyyMMdd-HHmmss-xxxx`: local stamp plus 4 random base36 characters, so two captures in one second never collide. "Latest" means newest `createdAt`, ties broken by `id`.
- `image.crop` is the crop rectangle in global screen points, so an agent can place `element.frame` inside the image.
- `ocr` and `iterations` are always `null` and `[]` in v0.1. They exist so the v0.2 MCP server and v0.4 Verify can add data without an app change.
- `path` is the nearest 6 ancestors, root-first, followed by the element itself (max 7 entries).
- `identifierSource` is `possiblySymbolName` when role is `image` and the identifier matches `^[a-z0-9]+(\.[a-z0-9]+)*$` (SF Symbol naming); `declared` for any other non-empty identifier; `unknown` when empty. (Observed: an SF Symbol image reported `AXIdentifier = plus`; agents must not grep for that as if a developer declared it.)
- `mode` is `fix` when: the front app's bundle id is in `MyApps` (hard-coded array in v0.1); or the front app is Simulator and the simulated app's bundle id is in `MyApps` or could not be read; or URL host is localhost/127.0.0.1. Else `reference`. Simulator running someone else's app is `reference`.
- `url` is always null in v0.1. The Safari/Chrome read (AppleScript, Automation prompt, usage string) is deferred to v0.2. The localhost rule stays implemented for when it arrives.

## 5. Payload (Markdown on clipboard)

Fix mode:

```
## Locant capture (fix)
Image: /Users/malik/Pictures/Locant/locant-simulator-20260912-140312-k7q2.png
App: Simulator (com.malikzhang.oryne) · Window: iPhone 17 Pro
Captured: 2026-09-12 14:03 · Image region: 144×112 pt @2x (element + 40 pt)

### Target element
button "Capture" · id=captureButton
Frame: x=312 y=88 w=64 h=32
Path: group > toolbar > button#captureButton

### Note
make this rounded, match the other toolbar buttons
```

Reference mode leads with `### Note`, then source, then element if any.

When `identifierSource` is `possiblySymbolName`, the element line carries a caveat:

```
image · id=plus (may be a symbol name, not a declared identifier)
```

When `identifier` is null, the element line ends with `· no identifier` and a hint line follows, so the missing id is fixed in the same loop:

```
button "Capture" · no identifier
No identifier. Grep the label text; add .accessibilityIdentifier("…") to this view so the next capture is exact.
```

When `element` is null:

```
### Target element
No element information available (app exposes no accessibility tree). Use the image.
If this view is yours, give it .accessibilityElement() and .accessibilityIdentifier("…") so Locant can point at it next time.
```

The `Image:` line is first because terminal agents (Claude Code, Codex CLI) receive only the text representation on paste; they read the PNG from disk. Claude Code accepts the path directly; Codex uses `view_image`.

## 6. Requirements

**R1 Hotkey.** Double-tap Control within 350 ms opens the overlay. Implemented with `NSEvent.addGlobalMonitorForEvents(matching: .flagsChanged)`. Requires no permission beyond Accessibility (already needed).

**R0 Visuals.** All colors, type, spacing, and motion come from `docs/PRD.md` section 6 (Design language), tokens table and surface specs for Overlay, Note field, Toast, No-element, Menu bar, Permissions. Do not invent values; if a token is missing, ask.

**R2 Overlay.** One borderless, transparent, non-activating `NSPanel` per screen, `level = .screenSaver`, `canBecomeKey = true`, over the live screen under `dim`. No frozen frame: pixels and tree cannot disagree, and the front app keeps focus so menus stay open and paste lands where it should. Hovering highlights the AX element under the cursor (frame from `ElementResolver`, throttled to 30 Hz) with `highlight.stroke`, and the `label.bg` label beside it reads identifier quality before the click: `button · captureButton` (identifier in `label.mono`), `button "Capture" · no identifier` (suffix in `secondaryLabelColor`), `image · plus · may be a symbol name`, or the no-element state `no element info · image only` with `highlight.fallback`. Click confirms. Esc cancels with no side effects.

**R2a Hover precision.** The platform hit test returns the deepest element under the point, and in SwiftUI every gap between controls belongs to a window-sized group, so naive hovering flickers between a control and the whole screen. Rules: (1) a hit on a container role (`group`, `window`, `toolbar`, `scrollArea`, `list`, …) is refined by searching its descendants near the point and taking the smallest real control whose frame grown by 8 pt contains the cursor; a smaller container is the next choice; the container itself stands only in a blank area or at an edge. (2) The last real control stays selected while the cursor is within 12 pt of its frame. (3) Option steps the selection to the parent, again to the grandparent, and wraps back; the label shows `↑n`. Clicking commits what is highlighted. (4) Flat trees: SwiftUI exposes leaves but not the cards that hold them, so when the hovered element's parent spans the window (≥ 50 percent of it), the parent's children are grouped by proximity (24 pt gap) into visual clusters; Option from a leaf goes leaf → cluster → parent, and hovering a blank spot inside a cluster selects the cluster. A cluster is role `cluster`, rawRole `LocantCluster`, identifier null, with `members` listing what it contains; the payload says it is computed, not an accessibility element. (5) A blank spot outside any cluster is "nothing specific here": the fallback square, `element: null` on click, and the window-sized group only via Option. Ancestors without a usable frame (the Simulator's application element) are never a level. The choice rules are pure functions in `HitRefiner` with tests.

**R3 Element resolution.**
- Primary path: on hover and on click, find the topmost normal-layer window under the point via `CGWindowListCopyWindowInfo`, excluding Locant's own windows, and take its owner PID; fall back to the frontmost PID recorded at hotkey time (R5). Call `AXUIElementCopyElementAtPosition` on `AXUIElementCreateApplication(pid)`. The per-app query ignores any window layered above the target, including Locant's own overlay, so the overlay never needs to hide for resolution. Every visible window is an honest target rather than a null hit.
- Fallback: the system-wide element (`AXUIElementCreateSystemWide()`), only if the per-app query returns nil.
- Electron and Chromium apps (Claude, VS Code, Slack, Chrome, Arc, …) expose only window-sized groups until an assistive client switches accessibility on. On first contact with an app in a session, set `AXManualAccessibility` (Electron, detected by the attribute) or `AXEnhancedUserInterface` (Chromium browsers, by bundle id) to true; the tree fills in within about half a second. Chromium answers the first hit test at a point from a cache and refines it asynchronously, so a container hit on the first read is re-read once (measured Sep 13: 3 of 7 control centers exact on the first read, all reachable through the tree).
- Lazy-tree retry: if the hit returns nil, or a `group` with zero children, retry every 100 ms up to 5 times before recording `element: null`. (Observed Sep 13: the Simulator's app tree was empty on first read and had 45 nodes seconds later; iOS builds it on first client access.)
- Read `AXRole`, `AXSubrole`, `AXTitle`, `AXDescription`, `AXValue`, `AXIdentifier`, `AXFrame`. Frames come back in global screen points; no conversion needed. Walk `AXParent` up to 6 times. Map to `ResolvedElement`, including `identifierSource` (section 4). All AX calls run on a background actor; overlay stays responsive.

**R4 Crop.** At click time, capture the element's frame plus 40 pt padding on each side with `SCScreenshotManager.captureImage` and `SCContentFilter(display:excludingWindows:)` excluding Locant's own windows, clamped to the target window's bounds and then to the display. The padding shows neighbors, since the job is layout polish. When `element` is null, the crop is a 120 pt square around the click point plus the same padding. The crop rectangle is recorded in `image.crop`. This is the PNG. (Full-window capture is deliberately not offered; Appshots does that.)

**R5 Context.** Collected at hotkey time, before the overlay appears, as the fallback. On click, when the clicked window belongs to a different app than the one that was frontmost, the source is re-collected for that app, so `source`, `mode`, and the window title describe what was pointed at (observed Sep 13: Finder frontmost, Simulator clicked, payload said Finder/reference). Simulator app bundle id comes from the most recently started process under `CoreSimulator/Devices/…/Containers/Bundle/Application`; null when none. No URL read in v0.1.

**R6 Note.** After click, a single-line `NSTextField` appears anchored below the element's frame, styled per the PRD note-field spec. Enter commits; empty note allowed. Esc cancels the whole capture: nothing on disk, clipboard untouched. Only Enter writes. The hint therefore reads `↩ copy · esc cancel` (the PRD's `esc skip` predates this rule).

**R7 Write.** `FileStore` writes PNG and JSON to `~/Pictures/Locant/` (created if missing), as `locant-<slug>-<id>.png` and `.json`. Filename slug is the app name lowercased, non-alphanumerics removed, max 16 chars.

**R8 Clipboard.** One `NSPasteboardItem` with `.png` and `.string` (Markdown). Never overwrite the clipboard on failure.

**R9 Failure feedback.** `CaptureError` cases: `.noAccessibilityPermission`, `.noScreenRecordingPermission`, `.noElementUnderCursor`, `.captureFailed(Error)`. Permission errors open the relevant System Settings pane once. `.noElementUnderCursor` still produces a capture with `element: null` (R5 text). `.captureFailed` shows a 1-second toast and leaves clipboard and disk untouched.

**R10 Menu bar.** Status item with the app name, "Capture (⌃⌃)", "Open capture folder", "Quit". Nothing else.

## 7. Tests (must pass before tag)

`ElementResolverTests` (fixtures recorded from the spike, stored as JSON dictionaries of AX attributes):
1. SwiftUI button with identifier → `role=button`, `identifier=captureButton`
2. AppKit text field without identifier → `role=textField`, `identifier=nil`, label from `AXTitle`
3. Unknown role `AXCustomThing` → `role=unknown`, `rawRole` preserved
4. Path truncates at 6 ancestors, root-first order
5. Missing `AXFrame` → resolver returns nil, not a zero frame
6. Simulator element path includes the simulator's own window group and still resolves the app's identifier
7. Empty first read followed by a populated read → resolver returns the populated element (stubbed provider exercises the retry path)
8. `image` with identifier `plus` → `identifierSource = possiblySymbolName`; `button` with `captureButton` → `declared`

`MarkdownBuilderTests`:
1. Fix mode ordering (image line first, element before note)
2. Reference mode ordering (note first)
3. Null element renders the "No element information" block
4. Empty note omits the `### Note` section entirely
5. Identifier with `#` and quotes is not escaped into something un-greppable
6. `possiblySymbolName` renders the caveat; `declared` does not
7. Null identifier renders `· no identifier` and the hint line

`GeometryTests`:
1. AppKit → CG point flip round-trips on the primary display
2. CG rect → AppKit rect flip
3. Crop = element + 40 pt, clamped to the window, then to the display
4. Pixel rect on a secondary display with negative origin and scale 2
5. Window hit-test picks the topmost layer-0 window under the point and skips Locant's own

`FileStoreTests`:
1. Slug: lowercase, non-alphanumerics removed, max 16
2. Id matches `yyyyMMdd-HHmmss-xxxx`

`HitRefiner` (in `ElementResolverTests`):
1. Smallest real control within 8 pt beats a nearby container and a far control
2. Only a smaller container nearby → that container; blank area → nil (container stands)
3. Stickiness keeps controls within 12 pt, never containers
4. Option depth re-roots the snapshot on the n-th ancestor; nil past the root
5. Clusters: siblings group by 24 pt proximity, backgrounds dropped, union frame
6. Levels from a leaf: leaf → cluster → group → window, frameless application skipped
7. Levels on a spanning container: cluster under the point, else nothing; then the container
8. A zero-size frame resolves to nil

`MarkdownBuilderTests`: 8. Cluster renders members, JSON carries `members` only for clusters; 9. Null element renders the identifier hint

## 8. Acceptance (the demo)

1. Launch Locant, grant Screen Recording and Accessibility.
2. Run Oryne in Simulator.
3. ⌃⌃, hover shows highlight and the label `button · captureButton`, click, type "make this rounded", Enter.
4. Paste into Claude Code with the prompt "do what the note says".
5. Claude Code edits the view file that declares `captureButton` and nothing else.
6. Paste into Preview: the cropped PNG appears.
7. `~/Pictures/Locant/` contains one PNG and one JSON; the JSON validates against `schema/capture.schema.json`.
8. Repeat with Figma as the front app: mode is `reference`, element is null or low-value, payload still has image path and note.
9. Click, then Esc: nothing written, clipboard unchanged.

## 9. Non-goals restated

Window capture, drag region, floating ball, OCR, color, cutout, lifecycle cleanup, Settings, URL scheme, MCP server, Raycast, App Store. Each has a place in the PRD; none is in v0.1.

## 10. Decisions deferred to v0.2

- Build the MCP server (TypeScript, `npx locant-mcp`) reading the JSON sidecars. Tools: `list_captures`, `get_capture(id, include_image=false)`, `resolve_capture(id)`. Return text plus path by default; image block only when `include_image` is true; never combine `structuredContent` with an image block (Codex bug).
- Evaluate reusing Peekaboo's `see` for AX parsing versus keeping `ElementResolver`. Default: keep our own; it is 100 lines and testable.
- Drag selection with multi-element resolution.
- Hotkey and folder in Settings.
- **Parked (Malik, Sep 13): draw a frame, identify what is inside.** Same gesture as ⌘⇧4; a click without drag stays a point capture. On mouse-up, walk the app under the drag start with frame pruning, keep controls at least 50 percent inside, cap 12, reading order. The drawn frame is the crop, no padding. Contract is additive: `element` stays the primary, `elements[]` is new, `image.crop` is the frame. Best slot: the first slot of v0.2, before the MCP server, so the server is written once against the final v1 shape and the MCP `get_capture` text lists elements from day one. Do it earlier (v0.1.1) only if the demo shows layout notes still need describing.

## 11. README skeleton (write in the last 15 minutes)

- One line: "Point, don't describe."
- What it does (three sentences), demo GIF placeholder.
- Install: download dmg, drag to Applications, grant Screen Recording and Accessibility (explain why: capture, and reading the element under your cursor; nothing leaves the machine).
- Agent compatibility table: Claude Code (tested), Cursor (tested), Codex (paste works; image via path), Gemini/Antigravity (untested).
- Known limitations: AX quality varies by app; Electron and custom-drawn UIs fall back to image plus note; Simulator support status from the spike.
- Build from source: `xcodebuild`, ad-hoc signing note.

## 12. Amendments (Sep 13, 2026, after critique, before 0:00)

- Live transparent overlay replaces the frozen frame: pixels and tree cannot disagree, no pre-capture, no 150 ms cold-start hope. The `dim` token applies to the live screen.
- Target PID comes from the window under the cursor, frontmost PID as fallback. Every visible window is an honest target.
- Hover label shows identifier quality before the click, using only the PRD §6 tokens (no colors of our own).
- Esc after click cancels; only Enter writes. Hint reads `esc cancel`.
- Crop is element + 40 pt, clamped to the window; `image.crop` recorded. (PRD P0.6 still says 8 pt; spec wins.)
- Simulator is `fix` only when the simulated app is in `MyApps` or unknown.
- `id` gains a 4-character suffix; "latest" is defined.
- Schema merged with the PRD: `rawRole` always present, `ocr` and `iterations` optional and empty.
- Safari/Chrome URL read deferred to v0.2 to pay for the above.
- Coordinate pipeline is a pure function with tests.
- Test count in the time plan corrected to 8; the probe script is a scratch file, not `axprobe.swift`.
- R0 Visuals added by Malik: PRD §6 is the source for every token and surface.
- Source follows the clicked window's app; hotkey-time context is only the fallback (found in the first end-to-end run).
- Hover precision (R2a) after the first hands-on run: container refinement, 12 pt stickiness, Option for parent.
- Electron/Chromium accessibility switched on per app; first container hit re-read once (after hands-on run on the Claude desktop app).
- Visual clusters for flat SwiftUI trees, blank spots are `null` not the whole screen, frameless ancestors skipped, identifier hint on null (after the Shortcut card and orb runs). MyApps corrected to the real bundle ids.
- Cluster frame is the members' bounds plus 16 pt inside the container (the visible card). Hover reads are cached for 400 ms per element and per container: a Simulator attribute read costs about 2 ms, and an uncached hover spent over 200 ms in reads.
