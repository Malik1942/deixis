{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/malikzhang/deixis/schema/capture.schema.json",
  "title": "Deixis capture",
  "description": "One capture: the element the user pointed at, the cropped image on disk, and the context it came from. This file is the only interface between the Deixis app and anything else. The Markdown on the clipboard is derived from it, never the other way around. Field names are platform-neutral.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "id", "createdAt", "mode", "image", "source", "element", "note", "resolved"],
  "properties": {
    "schemaVersion": { "const": 1 },
    "id": {
      "type": "string",
      "pattern": "^[0-9]{8}-[0-9]{6}$",
      "description": "Local timestamp, yyyyMMdd-HHmmss. Same stamp as in the PNG and JSON file names."
    },
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 with the local UTC offset."
    },
    "mode": {
      "type": "string",
      "enum": ["fix", "reference"],
      "description": "fix: the user's own code is on screen (front app is Simulator, bundle id is in MyApps, or URL host is localhost/127.0.0.1). reference: anything else."
    },
    "image": { "$ref": "#/$defs/image" },
    "source": { "$ref": "#/$defs/source" },
    "element": {
      "oneOf": [{ "$ref": "#/$defs/element" }, { "type": "null" }],
      "description": "null when resolution failed. Never fabricated."
    },
    "note": {
      "type": "string",
      "description": "The user's note. Empty string when the note was skipped."
    },
    "resolved": {
      "type": "boolean",
      "description": "Always false when written by the app. Reserved for an agent or the MCP server to mark the capture as acted on."
    }
  },
  "$defs": {
    "image": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path", "widthPt", "heightPt", "scale"],
      "properties": {
        "path": { "type": "string", "description": "Absolute path to the cropped PNG." },
        "widthPt": { "type": "number", "exclusiveMinimum": 0, "description": "Crop width in screen points." },
        "heightPt": { "type": "number", "exclusiveMinimum": 0, "description": "Crop height in screen points." },
        "scale": { "type": "number", "exclusiveMinimum": 0, "description": "Backing scale factor of the source display (2 on Retina). Pixel size is points times scale." }
      }
    },
    "source": {
      "type": "object",
      "additionalProperties": false,
      "required": ["app", "window", "url", "simulator"],
      "properties": {
        "app": {
          "type": "object",
          "additionalProperties": false,
          "required": ["bundleId", "name"],
          "properties": {
            "bundleId": { "type": "string" },
            "name": { "type": "string" }
          },
          "description": "The frontmost app at hotkey time."
        },
        "window": {
          "oneOf": [
            {
              "type": "object",
              "additionalProperties": false,
              "required": ["title"],
              "properties": { "title": { "type": ["string", "null"] } }
            },
            { "type": "null" }
          ],
          "description": "The front window at hotkey time. null when there is none."
        },
        "url": {
          "type": ["string", "null"],
          "description": "Front tab URL. Filled only for Safari and Chrome; null otherwise."
        },
        "simulator": {
          "oneOf": [
            {
              "type": "object",
              "additionalProperties": false,
              "required": ["device", "appBundleId"],
              "properties": {
                "device": { "type": "string" },
                "appBundleId": { "type": ["string", "null"] }
              }
            },
            { "type": "null" }
          ],
          "description": "Present only when the front app is the iOS Simulator."
        }
      }
    },
    "element": {
      "type": "object",
      "additionalProperties": false,
      "required": ["role", "label", "identifier", "identifierSource", "value", "frame", "path"],
      "properties": {
        "role": { "$ref": "#/$defs/role" },
        "rawRole": {
          "type": "string",
          "description": "The platform's own role string (for example AXCustomThing). Present only when role is unknown."
        },
        "label": { "type": ["string", "null"], "description": "Human-readable name: title or description." },
        "identifier": { "type": ["string", "null"], "description": "Developer-facing identifier as exposed by the platform, unmodified." },
        "identifierSource": {
          "type": "string",
          "enum": ["declared", "possiblySymbolName", "unknown"],
          "description": "possiblySymbolName: role is image and the identifier matches ^[a-z0-9]+(\\.[a-z0-9]+)*$ (SF Symbol naming), so it may not be a declared identifier. declared: any other non-empty identifier. unknown: identifier is empty."
        },
        "value": { "type": ["string", "number", "boolean", "null"] },
        "frame": { "$ref": "#/$defs/frame" },
        "path": {
          "type": "array",
          "items": { "$ref": "#/$defs/pathEntry" },
          "maxItems": 7,
          "description": "Ancestry, root-first: at most the nearest 6 ancestors, ending with the element itself."
        }
      },
      "if": { "properties": { "role": { "const": "unknown" } } },
      "then": { "required": ["rawRole"] }
    },
    "role": {
      "type": "string",
      "pattern": "^[a-z][A-Za-z0-9]*$",
      "description": "Lowercase, platform-neutral role: button, textField, staticText, image, group, toolbar, navigationBar, unknown, and so on. Never AX-prefixed."
    },
    "frame": {
      "type": "object",
      "additionalProperties": false,
      "required": ["x", "y", "w", "h"],
      "properties": {
        "x": { "type": "number" },
        "y": { "type": "number" },
        "w": { "type": "number", "minimum": 0 },
        "h": { "type": "number", "minimum": 0 }
      },
      "description": "Global screen points, top-left origin (accessibility convention)."
    },
    "pathEntry": {
      "type": "object",
      "additionalProperties": false,
      "required": ["role", "identifier"],
      "properties": {
        "role": { "$ref": "#/$defs/role" },
        "identifier": { "type": ["string", "null"] }
      }
    }
  }
}
